spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/consultorios_db
    username: consultorios_user
    password: secret123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
